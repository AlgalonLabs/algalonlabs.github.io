# Algalon Labs

Static GitHub Pages site for Algalon Labs.

## Local workflow

```bash
npm install
npm run lint
npm test
```

The site is intentionally framework-free and deploys as static assets through GitHub Pages.

The implementation is centered on a single self-contained `index.html` with inline CSS and JavaScript.
